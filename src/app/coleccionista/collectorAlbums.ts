export class CollectorAlbums {
  id: string;
  price: string;
  status: string;
  
  
  public constructor(id: string, price: string, status: string) {
    this.id = id;
    this.price = price;
    this.status = status;
  }
}
